# Manifiesto de prueba: DEBE ser rechazado por disallow-root.yaml.
# kubectl apply -n sa-p5 -f P8/security/kyverno/test-invalid-root.yaml
apiVersion: v1
kind: Pod
metadata:
  name: test-invalid-root
  namespace: sa-p5
spec:
  containers:
    - name: test
      image: ghcr.io/emileon9/sa-platform/gateway:v1.0.0
      resources:
        requests: {cpu: 50m, memory: 64Mi}
        limits: {cpu: 100m, memory: 128Mi}
