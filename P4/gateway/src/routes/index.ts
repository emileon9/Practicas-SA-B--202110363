import { Router } from "express";

// TODO(Fase 3/5): montar aqui las rutas hacia cada microservicio
// (users.routes, products.routes, orders.routes, notifications.routes)
export const router = Router();
